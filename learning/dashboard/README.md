# Local Learning Dashboard

Open `index.html` locally. The default homepage is the first-class Mechanism & Signal Map; Research Stage Map remains the canonical project-progress view. Mechanism cards open M0–M3/U drill-down pages, and the Signal Model Registry can be filtered by role. Concepts / Models and Papers remain supporting views. The dashboard contains no external resources and is not deployed.
