# S3 capture/cost scenario direction — researcher selection

Decision ID: S3-CAPTURE-COST-SCENARIO-DIRECTION-2026-09-22.
Status: SELECTED DESIGN DIRECTION ONLY.
Authority: direct researcher request “S3 CAPTURE + COST GATE — DEVELOP TRANSPARENT JOINT SCENARIO DECOMPOSITION”.
Ancestry: [capture/cost gate](../stages/G5/S3_CAPTURE_COST_DESIGN_CHECKPOINT.md), [threshold preregistration](S3_REFERENCE_TAIL_PROBABILITY_PREREGISTRATION.md), [authority ledger](../../research/POST_V1_RESEARCHER_AUTHORITY_LEDGER.md).

The researcher selects transparent joint scenario decomposition as the preferred S3 capture/cost design direction. Direct conditional gross-payoff forecasting is not selected for the primary design at this stage and is preserved only as a documented alternative.

This selection authorizes documenting a small interpretable prospective scenario model and its bounded alternatives. It does NOT approve detailed outcome states, state boundaries, probabilities, payoff magnitudes, duration law, cost conventions, probability-estimation architecture, economic admission rule, pooling, development specification or numerical parameters. Those remain proposals in the [scenario-model checkpoint](../stages/G5/S3_CAPTURE_COST_SCENARIO_MODEL_DESIGN_CHECKPOINT.md).

The primary statistical gate remains alpha=0.05 and abs(z_t)>=Q^w_0.95(abs(z_ref)), using the approved scale and weighting. Numerical z0 is uncomputed; the finite-grid threshold fallback is inactive. No alternative tail probabilities are introduced.

No historical data access, fitting, probability/duration/payoff/cost estimation, numerical quantile calculation, gate implementation, hedge/borrow implementation, acquisition, C04 repair, backtesting or PnL inspection is authorized. Frozen V1, approved scale/threshold decisions and historical exposure remain unchanged.

The current persistence instruction permits only this decision and its documentation/control consequences. NEXT_ACTION remains NONE after closeout. Detailed scenario choices require explicit researcher approval and future empirical work requires a separate bounded action.

**S3 CAPTURE + COST SCENARIO MODEL / RESEARCHER DECISION REQUIRED**
