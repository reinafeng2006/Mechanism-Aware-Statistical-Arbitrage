# G4-03B3 Nested Fold and Boundary Geometry Freeze

Decision date: 2026-09-10
Status: **APPROVED / FROZEN**

## Outer and inner geometry

- `OF4 — PRIMARY OUTER-FOLD ARCHITECTURE`: annual outer pseudo-OOS folds for 2020, 2021, 2022, and 2023;
- 2015–2019: initial development and inner-selection support;
- 2024–2025: final sealed held-out;
- `SEMIANNUAL CALENDAR-BOUNDARY ANCHORED INNER ORIGINS`: primary inner-origin structure;
- annual inner origins: registered lower-frequency sensitivity only;
- OF3 and OF6: registered sensitivity architectures only.

Sensitivity registration is not execution authorization and cannot create an alternative tuning environment.

`inner-origin cadence != relationship-refresh cadence`.

`automatic temporal updating under a frozen rule != manual retuning`.

At every outer origin, only prior authorized information may enter the frozen inner procedure. Manual revision after earlier outer inspection contaminates later folds under the same protocol.

## Outcome and overlap boundary

`TARGET-HORIZON-AWARE OUTCOME CENSORING` is selected. For origin `t` and horizon `O_h`, the entire outcome interval must remain within the same authorized temporal role. No outer outcome crosses into 2024–2025. Censoring changes outcome-role eligibility only, not raw records or event-time eligibility.

Purge/embargo is `CONDITIONAL / STRUCTURALLY TRIGGERED`; numerical lengths await frozen target/label overlap and dependence rules.

## Remaining boundaries

Inner/outer aggregation, winner-selection rules, rolling/expanding behavior, exact purge/embargo lengths, detailed censoring implementation, and native/common-support aggregation remain unresolved. All sensitivity execution requires later explicit inclusion in the comparison/sensitivity budget.
