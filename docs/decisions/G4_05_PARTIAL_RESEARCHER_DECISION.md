# G4-05 Partial Researcher Decision

Decision date: 2026-09-14  
Status: **PARTIALLY APPROVED / EXECUTABLE SPECIFICATIONS STILL GATED**

## Approved dispositions

- `R2-LIS` is the initial R2 equation architecture. OLS is primary and Ridge is the sole regularized challenger.
- `R2-N` is `DEFERRED / NOT REJECTED / NOT INITIAL EXECUTION`. It may be activated only after a separately preregistered functional-form inadequacy rationale and explicit researcher authorization; poor R2-LIS performance is not such authorization.
- the Gaussian empirical-Bayes random-intercept/random-slope R3 family and P0-only initial scope are approved in principle. The executable variance-component, shrinkage, origin-refresh, uncertainty, and PIT-ordering contract remains unfrozen.
- static intercept plus random-walk slope is the primary R4 architecture in principle. The mean-reverting-slope version is registered/deferred and not initially executed. Its executable variance, initialization, clock, and filtering contract remains unfrozen.
- `R5-EG-ECM` is `REGISTERED / BLOCKED PENDING QUALIFIED C04 AND PRICE-CONSTRUCTION CONTRACT`. VECM is deferred. No C04 workaround or fitting is authorized.
- a documentation-only, existing-source P1 fundamentals field/PIT qualification audit is authorized. It authorizes neither acquisition nor provider expansion.

## Continuing prohibitions

No model fitting, P1 acquisition, provider addition, empirical-distribution or coverage inspection, relationship/abnormality/mechanism/target/return/PnL computation, A6 work, Search-Budget amendment, or 2024–2025 held-out access is authorized.

The approvals above do not satisfy the computation gate. Any candidate with an unresolved equation binding, estimator implementation, PIT input, pooling semantics, update rule, H/U execution subset, or Search-Budget entry remains `NOT COMPUTATION-AUTHORIZED`.
