# V1 Directional Episode-Collision Integrity Blocker

Date: 2026-09-14
Status: **GENUINE DETERMINISTIC-EXECUTION BLOCKER / RESEARCHER DECISION REQUIRED**

`DC-A + EP-A` is frozen and published. DC-A preserves separate `i -> j` and `j -> i` event records. EP-A permits only one active episode per `unordered pair × policy channel`, so direction is deliberately absent from the uniqueness key.

At one candidate-eligible session, both directional records can mechanically satisfy the same M1 or M2 entry gate. The frozen protocol does not say which single episode is admitted. Stacking is prohibited; episode-layer netting is prohibited; resetting/replacing is prohibited; selecting the larger abnormality would introduce an unauthorized ranking rule. This ambiguity changes positions and PnL and cannot be a library default.

No fit, abnormality, target, trade, or PnL was computed. OF4 and held-out were not accessed.

## Bounded alternatives

- `DECA-A — DUAL-DIRECTION SAME-CHANNEL COLLISION IS TRADING-INELIGIBLE` (recommended): preserve both scientific records; create no episode; record `DIRECTIONAL ENTRY CONFLICT`. This uses no new magnitude threshold or ranking and is consistent with the frozen permission for unresolved conflict to block trading.
- `DECA-B — ONE TWO-LEG PAIR-POLICY EPISODE`: combine both qualifying directional instructions into one episode before portfolio netting. This changes the frozen single-leg M1/M2 policy interpretation and requires exact leg sizing.
- `DECA-C — DETERMINISTIC DIRECTION PRIORITY`: admit one direction by a preregistered priority statistic. This introduces a new ranking metric/tie rule and would require independent justification.

Recommended: freeze `DECA-A`. If exactly one direction qualifies for the pair/channel, admit it under EP-A. If both qualify simultaneously, retain both event records, create no episode or exposure, and record `DIRECTIONAL ENTRY CONFLICT`. M1 and M2 channels remain separate; a collision in one channel does not erase an independently valid other-channel episode.

`V1 DIRECTIONAL EPISODE COLLISION / RESEARCHER DECISION REQUIRED`
