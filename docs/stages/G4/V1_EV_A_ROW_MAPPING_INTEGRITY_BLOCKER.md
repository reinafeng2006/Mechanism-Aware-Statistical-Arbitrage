# V1 EV-A Row-Mapping Integrity Blocker

Status: `RESEARCHER DECISION REQUIRED`

The approved `EV-A-V1` contract defines conservative field-level truth semantics, but the repository does not yet contain an executable, source-bound rule that determines those states for each event. In particular, the current contracts do not establish an exhaustive V1 source or row-level derivation for contextual M1 support/rivals, evidence conflict, M0 negative determination, or relationship-break evidence.

Two attempted implementations that globally encoded these fields as unavailable were rejected by the workspace approval control because doing so would predetermine every A6 fit and G5 trade disposition rather than execute the authorized row-level mapping. No workaround was used.

## Completed and preserved

- All 70 A3/A5 inner partitions were generated from the immutable relationship and RT3 ancestors.
- The first generation was discarded before publication after structural validation found that its target censoring used year rather than semiannual boundaries.
- The corrected generation enforces O1/O5/O10/O20 wholly within each semiannual inner role.
- All corrected artifacts passed SHA-256, ancestry, row-shape, and boundary-censoring validation.
- No relationship/RT3 artifact was recomputed or changed; OF4 and held-out were not accessed.

## Exact unresolved execution decision

The researcher must approve a source-bound rule for deriving each EV-A-V1 state at event-row grain, or explicitly approve the conservative global-unavailability disposition and its consequences. Absence of a qualified record cannot be silently converted to either `ABSENT` or `UNAVAILABLE` without binding the applicable source coverage/exhaustiveness contract.

Bounded alternatives:

1. `EV-MAP-A — SOURCE-BOUND ROW MAPPING`: provide/approve the existing qualified V1 sources and exhaustive-negative capabilities for each M1/rival/U/M0/break proposition; execute row-level mapping and continue A6/G5.
2. `EV-MAP-B — EXPLICIT GLOBAL UNAVAILABILITY FOR V1`: explicitly authorize unavailable states for the currently unqualified contextual propositions, accepting resulting A6 rank/row-availability and G5 eligibility consequences as governed outputs rather than inferred defaults.
3. `EV-MAP-C — DEFER A6/G5 V1`: preserve corrected A3/A5 outputs and stop downstream execution without changing evidence semantics.

Recommendation: `EV-MAP-A` if a qualified source-bound mapping already exists; otherwise `EV-MAP-B` is the transparent conservative V1 disposition. The decision cannot be deferred if A6/G5 V1 execution is to continue.

`V1 EV-A ROW MAPPING INTEGRITY BLOCKER / RESEARCHER DECISION REQUIRED`
