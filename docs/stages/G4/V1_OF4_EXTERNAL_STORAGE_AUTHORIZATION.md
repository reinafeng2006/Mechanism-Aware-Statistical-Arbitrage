# V1 OF4 External Storage Authorization

PO-C remains scientifically unchanged. Newly generated OF4 large payloads and their temporary materialization files are authorized under `D:\MechanismAwareStatArbData\OF4`. Physical storage location is not scientific artifact identity.

The preceding C-drive audit projected 338,737,015 logical rows, 88,221,512,657 uncompressed bytes, 35,473,741,022 losslessly compressed bytes and 49,723,741,022 bytes of safe peak capacity including atomic-write, manifest/checksum, restart and reserve allowances. C: had a 24,862,253,918-byte shortfall. The authorized D: root passed existence, write/read/checksum/delete probing and reported 808,974,237,696 free bytes (753.42 GiB).

Large payloads are excluded from Git. Repository manifests retain canonical logical ID, physical root, relative partition path, ancestry, schema/version, row count, SHA-256, compression, creation state and validation state. Existing core, inner, RT3 and A3/A5 artifacts remain unchanged in place.

Storage capacity cannot change the scientific design. Top-K filtering, sampling, pair-universe reduction, field deletion, lossy rounding, year reduction, or any other scientific-semantic modification is prohibited. Each partition must use deterministic lossless compression and `temporary write -> validate -> checksum -> atomic finalize`.

The sealed 2024–2025 held-out region is neither created nor accessed by this authorization.
