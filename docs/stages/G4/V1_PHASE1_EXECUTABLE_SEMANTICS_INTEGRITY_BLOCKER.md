# V1 Phase 1 Executable-Semantics Integrity Blocker

Status: **PHASE 1 SAFELY PAUSED BEFORE EMPIRICAL ACCESS / RESEARCHER DECISION REQUIRED**
Date: 2026-09-14
Trigger: mandatory stop in `V1-PAIR-A-PHASE1-INNER-EXECUTION-V1` for an unresolved numerical or scientific semantic required for execution.

## Completed before the stop

- `C06-AVAILABILITY-AMENDMENT-V1` was frozen and published without changing `CORE-DATASET-FREEZE-V1` or C06 membership contents.
- All ten 2015–2019 semiannual origins are structurally C06-PIT-constructible.
- The active action, frozen seven tuples, A1–A6, G5, PAIR-A, C04-A, and dataset/held-out guards were compared at executable-statement level.
- No C03/E02/E03-A value, return, realized pair count, model fit, abnormality, target, PnL, OF4 record, or held-out record was read or computed.

## Exact closure required

The high-level V1 bundle is frozen, but the following implementation-changing choices remain open in its controlling documents. Engineering defaults would change scientific semantics.

| Closure item | Existing frozen boundary | Missing executable binding | Why it is substantive |
|---|---|---|---|
| `E1 — A1 scale mathematics` | MAD primary; sample SD robustness; no epsilon; mathematical support is a hard state | finite-sample median convention, MAD consistency multiplier treatment, sample-SD degrees-of-freedom convention, and exact mathematical minimum support for each estimator | changes scaled losses, availability, and common support |
| `E2 — R1-MI cross-code factor binding` | leave-pair-out C06 industry factor; PAIR-A permits 34/35 cross-code pairs | whether each security uses its own contemporaneous industry factor, the response security's factor, a pair-composite factor, or another fixed mapping | changes residuals and the intended R1-M to R1-MI estimand |
| `E3 — R3 hierarchy for cross-code pairs` | one taxonomy-versioned C06 hierarchy; random intercept/slope; REML; N-ZERO | group identity for a 34/35 pair and direction (source code, response code, sorted code-pair, or whole machinery scope) | changes pooling and shrinkage, so it cannot be selected by software convention |
| `E4 — R3/R4 numerical estimation contract` | R3 unstructured covariance/group variance with explicit boundary failure; R4 PIT-ML Q/R and matched-static initialization | optimizer/parameterization and convergence identity, R3 finite-support/rank contract, R4 Q/R sharing unit, prior-slope variance formula, and deterministic boundary/convergence disposition | changes estimability, shrinkage, uncertainty, and native/common support |
| `E5 — A6 executable feature map` | PV0/PV-M1/PV-M2/PV-BOTH information sets and OLS are frozen | exact columns/transforms for continuous channels, encoding of categorical quality/U/contamination states, missing-channel handling, and full-rank disposition | different encodings are different predictive equations |
| `E6 — G5 evidence-to-entry map` | no trading-only threshold; entry requires a preregistered M1- or M2-supporting evidence state | exact rule converting UR0/UR1/context and MP0/MP1/contamination into “supporting,” “blocking rival,” and deterministic entry eligibility | A4 freezes `morphology != mechanism identification`; treating `UR0 > 0` or nonzero `MP0` as support would silently cross that boundary |

Two additional mechanical bindings should be stated in the same executable amendment: the exact E03-A benchmark identifier/version already present in the frozen manifest, and the scheduled calendar convention for U1W/U1M refresh dates. Those can be copied from authoritative frozen metadata/calendar semantics once the scientific closures above are selected.

## Bounded alternatives

### `EXEC-A — Conservative complete executable amendment` (recommended)

Freeze one explicit implementation contract for E1–E6 before any data access. Recommended structural direction, without treating it as authorization:

- standard deterministic finite-sample definitions for MAD/SD, with unsupported/zero states rather than floors;
- security-specific contemporaneous leave-pair-out industry factors in R1-MI;
- a single explicitly named directional or pair-code hierarchy for R3;
- one deterministic REML/ML parameterization, convergence policy, and support/rank contract;
- an enumerated A6 design matrix with fixed missing/state encoding;
- an A4-consistent evidence-to-entry rule that does not relabel morphology as mechanism identification.

Trade-off: closes the full SCI-A/TRADE-A path with the least ambiguity, but requires researcher choices for hierarchy, encoding, and mechanism-evidence use.

### `EXEC-B — Scientific relationship-only inner run`

Freeze E1–E4 and authorize R0/R1/R3/R4 plus A1–A5 inner outputs only; defer A6 and G5 until E5–E6 are independently frozen.

Trade-off: reaches relationship/abnormality evidence sooner and preserves A4, but does not satisfy the currently intended full pre-outer scientific-and-trading checkpoint.

### `EXEC-C — Reduced already-exact subset`

Authorize only candidates whose equations can be fully closed without R3/R4 hierarchy/variance choices, after E1/E2 are frozen; keep R3/R4/A6/G5 paused.

Trade-off: lowest immediate implementation risk, but changes the approved MODEL-A execution scope and therefore requires an explicit protocol amendment rather than an engineering shortcut.

### `EXEC-STOP — Keep Phase 1 paused`

Make no executable amendment and preserve all gates. No empirical access occurs.

## Recommendation and dependency

Choose `EXEC-A` and freeze one versioned executable-semantics amendment. E1–E4 are prerequisites for relationship computation; E5 is required before A6; E6 is required before any PnL. E5–E6 can be deferred only by explicitly selecting `EXEC-B`. None may be resolved from survival counts, fit quality, returns, pair counts, or outcomes.

After approval, the exact next action is: publish and validate the executable amendment, reactivate `V1-PAIR-A-PHASE1-INNER-EXECUTION` against that version, then implement and compute only 2015–2019 inner-development artifacts before the pre-outer gate.

`GENUINE INTEGRITY BLOCKER / RESEARCHER DECISION REQUIRED`
