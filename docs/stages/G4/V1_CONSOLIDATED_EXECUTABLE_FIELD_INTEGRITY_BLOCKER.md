# V1 Consolidated Executable-Field Integrity Blocker

Status: **GENUINE SCIENTIFIC/TRADING-SEMANTICS BLOCKER / RESEARCHER DECISION REQUIRED**

Date: 2026-09-15

The documentation-only scan followed publication of `V1-MP1-I-A-1.0`. It used no dataset values, relationship outputs, abnormality, outcomes, pair counts, trades, PnL, OF4, or held-out information.

## Mechanically closed

- A3 directional departure, candidate-neutral scaling, same-decision ordering, and invalid-scale propagation are executable from the frozen contracts.
- A5 RT0–RT3 equations, O1/O5/O10/O20 maturity/censoring, and event-time RT3 state are executable through the qualified augmentation.
- MP1 is now exactly one ratio-native state: `log((amount/volume)/median_PIT,H126,U1W(amount/volume))`; unsupported separate amount/volume normalizations are absent from the descendant interface.
- SG-A unresolved publication-regime state remains unavailable and propagates data/provenance uncertainty; it is never fabricated as Boolean zero or one.
- G5's primary execution object is next eligible open, which is already named by the frozen contract; no next-close performance branch is opened.

## Consolidated unresolved decisions

### 1. A6 event-time evidence-state encoding

The frozen matrix names, but does not define executable mappings for:

- PV0 U indicators: measurement uncertainty, information insufficiency, mechanism ambiguity, evidence conflict, and data/provenance uncertainty;
- positive M0/rejection and relationship-break/invalidity indicators;
- PV-M1 `M1_support`, `M1_oppose`, and `M1_ambiguous`;
- PV-M2 contamination indicators: mechanical return content, cause/proxy overlap, endogeneity, overlapping windows, future leakage, and flow-motive ambiguity.

Setting these fields from morphology alone would violate `measurement != evidence`; setting them all to zero would fabricate qualified evidence; setting all conceptual limitations to one would change the predictive information set and G5 eligibility. This is not schema plumbing.

Bounded alternatives:

1. **EV-A — conservative executable state map (recommended).** Freeze an explicit deterministic mapping from already available PIT structural/model states. Morphology never creates evidence by itself; unresolved rival/contamination dimensions remain explicit and block only where the existing G5 conjunction says they block. No external event data or new thresholds.
2. **EV-B — reduce V1 to morphology plus structural-quality predictors.** Version-amend A6 to omit unqualified evidence-direction/contamination fields and defer M1/M2 incremental evidence comparisons and corresponding G5 channels. This changes V1 scope.
3. **EV-C — defer A6/G5.** Materialize A3/A5 only and postpone predictive/trading execution until an independently qualified evidence layer exists.

### 2. G5 pending-fill maximum delay

The frozen G5 record permits an untradeable entry/exit to remain pending only under a separately frozen maximum calendar-delay rule, but no such duration or immediate-cancel disposition exists. The choice changes episode eligibility, realized execution prices, holding duration and PnL.

Bounded alternatives:

1. **FD-A — no delayed fill (recommended).** Require the next eligible session's authorized open; if it is not executable, mark the episode/fill unavailable rather than carrying an order. This adds no arbitrary duration.
2. **FD-B — carry through at most the existing 10 eligible-session episode clock.** Uses an existing V1 number but changes exposure timing and can consume the episode without entry.
3. **FD-C — defer G5 while A3/A5/A6 proceed.** Avoids a new trading convention but prevents the requested V1 economic checkpoint.

### 3. Economic-reporting clock

G5 requests annualized return, volatility and Sharpe, but does not bind the annualization clock. Choosing 252, realized sessions per year, or calendar-time scaling changes reported economic metrics, although not cash-flow PnL.

Bounded alternatives:

1. **ER-A — exchange-session annualization at 252 (recommended).** Fixed market-calendar convention; report unannualized period returns alongside it.
2. **ER-B — calendar-time annualization.** Uses elapsed calendar duration but is less aligned with eligible-session simulation.
3. **ER-C — no annualized metrics in the pre-outer checkpoint.** Report only unannualized path, drawdown, turnover and exposure; defer annualization.

## Recommendation and dependency

Select `EV-A + FD-A + ER-A`. It is the lowest-complexity executable closure: an explicit conservative evidence map, no invented delayed-fill window, and a fixed market-session reporting convention. The exact EV-A field map must be researcher-approved because it determines A6 predictors and mechanism/trading eligibility; FD-A and ER-A affect PnL construction/reporting. None may be selected from results.

After approval, publish one versioned executable amendment, run the field scan again, restore the inner-only action, and continue from the existing relationship/RT3 checkpoint without recomputation.

`V1 CONSOLIDATED EXECUTABLE FIELDS / RESEARCHER DECISION REQUIRED`
