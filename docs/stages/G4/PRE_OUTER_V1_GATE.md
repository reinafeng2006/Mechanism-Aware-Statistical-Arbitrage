# PRE-OUTER V1 Gate

Status: `RESEARCHER DECISION REQUIRED`

## Completed inner execution

- Published `EV-MAP-B` with qualified row-level exceptions and explicit global V1 unavailability only for unsupported propositions.
- Preserved all immutable relationship, RT3 and corrected A3/A5 artifacts without recomputation.
- Audited 70 partitions and 273,371,732 relationship-event rows; all passed the mathematical/lineage validity checks used by this stage.
- Materialized and checksummed A3/A5 targets under semiannual target-horizon censoring.
- Executed all 4,032 registered A6 candidate × origin × target × horizon × information-set estimability gates.
- Executed the frozen G5 evidence-quality admission gate using the non-identifying `M1-MOTIVATED DIRECTIONAL PROBE` and `M2-MOTIVATED DIRECTIONAL PROBE` terminology.

## Inner disposition

All A6 specifications are unavailable under the frozen design-matrix contract. Every information set inherits PV0; EV-MAP-B explicitly preserves constant-one `M0_unavailable` and `relationship_break_evidence_unavailable` columns together with the intercept. The resulting rank deficiency cannot be repaired by silently dropping columns or fabricating variation.

No G5 event passes the frozen requirement that there be no unresolved evidence-conflict/quality block. Consequently no episode, trade, turnover, annualized return, or PnL was created. This is not evidence against any mechanism or relationship candidate and does not interpret empirical performance.

## Researcher decision

1. `PO-A — PRESERVE V1 PROTOCOL / DO NOT OPEN OF4` (recommended): accept the conservative V1 non-estimability/no-admission disposition and close V1 downstream without OF4 or held-out access.
2. `PO-B — EXPLICIT PROTOCOL REOPENING`: version and justify a different estimability/encoding rule (for example, a preregistered treatment of structurally constant unavailable indicators) before recomputing A6/G5. This is a scientific protocol amendment and must record contamination lineage; it cannot be inferred mechanically.
3. `PO-C — RELATIONSHIP-STAGE OF4 ONLY`: separately authorize OF4 evaluation for the frozen relationship layer while retaining A6/G5 as unavailable. This narrows the V1 claim scope and still does not authorize held-out access.

The decision cannot be deferred if V1 is expected to produce A6 or trading evidence. No tracked work is left unpublished at this checkpoint after the associated commit succeeds.

`PRE-OUTER V1 GATE / RESEARCHER DECISION REQUIRED`
