# V1 Phase 1 Pair-Universe Integrity Blocker

Status: **GENUINE INTEGRITY BLOCKER / RESEARCHER DECISION REQUIRED**

Date: 2026-09-14

Boundary: no relationship fitting, abnormality, target, return, PnL, OF4, or held-out access occurred.

## Autonomous work completed

The approved C04-A official action-calendar descendant was acquired and validated without mutating `CORE-DATASET-FREEZE-V1`:

- manifest: `C04-A-OFFICIAL-CALENDAR-V1`;
- official raw artifacts: 195;
- normalized action rows and V1 exclusion records: 1,929 each;
- unresolved effective dates in acquired records: zero;
- acquisition failures: zero;
- action-calendar SHA-256: `61215BB425880B02A5E11F1C8DE2CE613BE73E15C31FD876AD85282E810E3E69`;
- exclusion-calendar SHA-256: `B63188E35EF76A3B8E5DAE4F02FE68CE2033ED5554113DBE30CEF3B3C993F910`;
- public manifest SHA-256: `402741A7BF767FD56508AC957DE12D2B422E2B0B80F30ABE0B47C03255AC267A`.

The descendant state preserves `NO IDENTIFIED ACTION UNDER V1 CALENDAR != AUTHORITATIVELY ACTION-CLEAN`; it never promotes the frozen core C04 state to clean and never uses an adjusted-price substitute.

## Exact unresolved scientific decision

The repository freezes the target industry universe, pair-information architecture, seven executable relationship tuples, eligibility, metrics, temporal geometry, and model estimators, but it does not freeze the **candidate pair universe/formation rule**. Existing decisions explicitly defer the pair universe. R0-DIST is permitted as possible screening context only under a later authorized pair rule.

Executing Phase 1 now would silently select at least all of the following:

- whether the candidate set is all unordered contemporaneously eligible in-scope pairs or a screened subset;
- whether cross-code `34`/`35` pairs are permitted;
- whether screening uses R0-DIST, R0-CORR, or another frozen representation;
- any screen size/rank/tie rule and its multiplicity consequences;
- how unordered pair membership maps to the already-frozen two directional responses.

Those choices change the scientific estimand, comparison family, computational burden, and multiplicity family. They are not mechanical implementation details.

## Bounded alternatives

| Alternative | Exact V1 rule | Benefit | Cost / risk |
|---|---|---|---|
| `PAIR-A — COMPLETE PIT ALL-PAIRS` | At each authorized origin, form every unordered pair whose two identities are simultaneously inside the PIT `34 + 35` universe and satisfy frozen candidate eligibility; evaluate both directions. | Most neutral; no outcome-free screening metric or arbitrary `K`; preserves cross-code possibilities. | Highest compute and multiplicity burden; many economically weak pairs remain candidates and must be handled by frozen validity evidence rather than pre-screening. |
| `PAIR-B — WITHIN-C06-CODE ALL-PAIRS` | Same as A, but require both identities to share the same contemporaneous C06 code/taxonomy version at the origin; evaluate both directions. | Lower burden and stronger coarse industry coherence. | Adds a new hard same-code assumption and excludes `34`–`35` relationships despite the frozen joint machinery scope. |
| `PAIR-C — PREREGISTERED REPRESENTATION SCREEN` | At each origin, screen on prior formation information only using one already-frozen representation, then retain an exactly specified rank/count with deterministic ties; evaluate both directions. | Most tractable and closer to conventional data-driven pair formation. | Requires new scientific selections for representation, `K`/retention, tie handling, refresh, and screening-family multiplicity; can privilege a representation that is also being compared. |

## Recommendation

Recommend `PAIR-A — COMPLETE PIT ALL-PAIRS` for V1 unless the researcher places an explicit computational constraint on Phase 1. It makes the fewest additional scientific assumptions and does not use a preregistered competitor to define its own comparison population. If its engineering estimate is unacceptable, select `PAIR-C` only with a fully frozen screen and Search-Budget/multiplicity amendment before any fitting.

The decision cannot be deferred if Phase 1 relationship fitting is to begin. C04-A itself is complete and does not need to be repeated.

## Exact next action after approval

Create one successor action that freezes the chosen pair-universe/formation rule, updates the comparison/multiplicity contracts mechanically, implements a temporal access guard that materializes only the authorized 2013–2019 formation/development inputs, and then resumes the already-authorized 2015–2019 inner-development pipeline from the preserved C04-A checkpoint.

`V1 PHASE 1 SAFELY PAUSED / PAIR-UNIVERSE DECISION REQUIRED`
