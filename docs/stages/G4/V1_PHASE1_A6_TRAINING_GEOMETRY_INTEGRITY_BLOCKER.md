# V1 Phase 1 A6 Training-Geometry Integrity Blocker

Date: 2026-09-14  
Status: **GENUINE SCIENTIFIC-INTEGRITY BLOCKER / RESEARCHER DECISION REQUIRED**

## Completed before the stop

The CF-A benchmark passed and its disposition and role guard were published before empirical access. A deterministic 2013-01-07–2019-12-31 inner-input descendant was then constructed from `CORE-DATASET-FREEZE-V1`, C04-A, and `C06-AVAILABILITY-AMENDMENT-V1`. It canonicalizes normalized duplicate payloads, applies the frozen C04 action-date exclusion, propagates C05/identity state, activates C06 snapshots only after qualified availability, and binds E03-A. The ignored artifact is checksum-bound by `V1-PHASE1-INNER-INPUT-1.0`. No pair screening, relationship fitting, outcome inspection, OF4 access, or held-out access occurred.

## Exact unresolved scientific decision

The frozen A6 contract specifies the 23-column PV0 matrix, PV-M1/PV-M2 additions, OLS, targets, horizons, censoring, and semiannual inner origins. It does **not** specify the executable training geometry needed to make an A6 forecast:

1. which fully elapsed semiannual inner-origin records train the model at each later origin;
2. expanding versus rolling training inside CG2 (explicitly left unresolved in G4-03B2);
3. the first inner origin at which A6 prediction is permitted, because 2013–2014 is warm-up/formation only and cannot silently become development-label evidence;
4. whether A6 is pooled across pair-direction records or fitted at another unit; and
5. whether ordinary OLS rows or deterministic pair-balanced weights govern fitting. The latter changes the estimator and determines whether observation-rich pairs dominate estimation, even though evaluation aggregation is equal-pair.

These are not numerical-library defaults. Each choice changes the predictive estimand, information set, and later G5 input. Continuing would violate the frozen prohibition on independently choosing temporal geometry, estimator semantics, and aggregation/weighting.

## Bounded alternatives

### `A6-TG-A — EXPANDING COMPLETED-INNER TRAINING` (recommended)

- 2015H1 generates relationship/A3/A5 records and, after every O20 interval has elapsed, becomes the first A6 training block.
- First A6 prediction is 2015H2; each later semiannual origin uses all fully elapsed, outcome-authorized earlier inner blocks.
- Fit one equation per relationship candidate × target component × horizon × PV information set, pooling directions while preserving direction fields and using deterministic equal-total-pair weighting.
- Best sample efficiency and live sequential realism; later periods have larger training sets, which must remain visible.

### `A6-TG-B — ROLLING COMPLETED-INNER TRAINING`

- Same no-warm-up-label and first-prediction boundary, but uses a fixed preregistered number of prior completed semiannual blocks.
- Better local adaptivity and constant-span interpretation; requires a new numerical block-span decision and discards earlier authorized evidence.

### `A6-TG-C — RELATIONSHIP-ONLY V1; A6/G5 DEFERRED`

- Execute R0/R1/R3/R4 and A1–A5 inner artifacts, but do not perform A6 or G5 in V1.
- Avoids selecting a predictive geometry now, but no PRE-OUTER predictive/economic gate can be completed.

## Why the decision cannot be inferred

The frozen high-level calendar deliberately left rolling versus expanding behavior inside CG2 unresolved. Semiannual origins define when evidence may be generated, not how labeled records are pooled into an estimator. Neither the 23-column matrix nor equal-pair evaluation aggregation supplies a training-weight rule. Selecting any alternative would therefore be a researcher-level temporal/estimator decision.

Recommended next action: freeze `A6-TG-A` exactly, including first prediction, fully elapsed target rule, pooling unit, and equal-total-pair fitting weights; then resume the existing Phase 1 action from the checksum-bound input without rebuilding it.

OF4 2020–2023 remains inaccessible. The 2024–2025 held-out region remains sealed.

`V1 PHASE 1 A6 TRAINING GEOMETRY BLOCKER / RESEARCHER DECISION REQUIRED`
