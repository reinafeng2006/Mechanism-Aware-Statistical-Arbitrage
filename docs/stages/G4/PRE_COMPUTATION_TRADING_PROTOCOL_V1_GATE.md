# Pre-Computation + Trading-Protocol V1 Gate

Status: **APPROVED / FROZEN — 2026-09-14**  
Mode: **V1 PROTOCOL FROZEN / PHASE 1 INNER EXECUTION AUTHORIZED SUBJECT TO C04-A**

## Completed autonomously

- closed V1 expansion: no new model, feature, mechanism, target, temporal, metric, sensitivity, or provider family;
- propagated R2-LIS/OLS/Ridge, R3, R4 and R5 decisions and produced executable R3/R4 bundles without adding families;
- completed the existing-source P1 field/PIT audit; accounting constructs remain unresolved and R2 is therefore `V1 NOT DATA-READY`, preserved for V2;
- proposed a seven-tuple relationship Search Budget for R0/R1/R3/R4 and retained R2-N, R4-MR, R5, VECM, directed linkage and MP3 outside V1;
- specified V1 UR0/UR1, MP0/MP1, M0/U boundaries without mechanism probabilities or labels;
- specified continuous A5 endpoint targets and a bounded A6 predictive-validation protocol;
- specified one minimal interpretable Trading Simulation V1 architecture, transaction-cost convention, exposure controls and economic metrics;
- audited leakage, P/N attribution, CS2, temporal boundaries, multiplicity, scientific/trading separation, and candidate completeness.

No research data was acquired, no model or target was computed, no empirical distribution/result/PnL was inspected, and OF4/2024–2025 remained sealed.

## Genuine integrity blocker — C04

Every frozen sidecar row remains `CORPORATE_ACTION STATUS UNRESOLVED`. Raw observations therefore cannot yet be silently treated as action-clean responses or economic holding-period PnL.

Select one:

1. **C04-A — authoritative action-calendar exclusion contract (recommended):** authorize bounded acquisition/reconstruction from the already frozen official SSE/SZSE/company-disclosure companion architecture; establish complete action coverage and mark action-affected intervals unavailable. Do not build a canonical adjusted-price series for V1.
2. **C04-B — authoritative adjusted-price construction:** first qualify distributions/actions and a full entitlement-adjustment method/version, then create a descendant price layer. Stronger economic continuity but materially slower and more complex.
3. **C04-STOP:** do not authorize either path; scientific return models and Trading V1 remain blocked.

Recommendation: C04-A is the minimum integrity-preserving path. It solves contamination by exclusion rather than pretending an opaque adjustment factor is PIT truth.

## Decision 1 — V1 relationship executable bundle

Approve or amend the proposed seven tuples in [the V1 registry](G4_05_V1_EXECUTABLE_SPECIFICATION_REGISTRY.md):

`R0-DIST H252/U1M; R0-CORR H126/U1W; R0-LIN H126/U1W; R1-M H126/U1W; R1-MI H126/U1W; R3 H252/U1M; R4 H63/U1D`.

Bounded alternatives:

- **MODEL-A — recommended full minimal V1:** all seven tuples; R3=`REML + N-ZERO`; R4=`PIT-ML Q/R + matched-static initialization + U1D eligible-session filtering`; Spearman/Huber diagnostics only.
- **MODEL-B — reduced static V1:** R0/R1 only. Lowest implementation risk, but does not test the approved pooling/dynamic questions.
- **MODEL-C — full minimal V1 with ML R3:** same as A but R3 marginal ML. Easier likelihood comparison, potentially more downward variance-component bias.

R3 `N-STRENGTH`, fixed/arbitrarily chosen R4 variance, inner-selected variance grids, extra H/U tuples and H504 are not recommended because they add an unresolved tuning axis. E03-A benchmark identity must be bound from the frozen manifest before implementation; if identity cannot be established structurally, R1-M/R1-MI remain unavailable rather than substituted.

## Decision 2 — V1 mechanism and A6 protocol

Select one:

1. **SCI-A — recommended bounded protocol:** UR0 primary continuous M1 morphology; UR1 calibration-dependent supporting evidence; MP0 plus separately retained MP1 amount/volume context; positive-only M0; five-dimensional U0/U4 state; endpoint RT0/RT1/RT2 and original-state RT3; OLS A6; O5/O10 confirmatory M1/M2 families; O1/O20 diagnostics; Holm FWER within each two-horizon confirmatory family.
2. **SCI-B — same but Ridge as the sole A6 challenger:** adds one inner-selected regularization branch; greater stability potential but enlarges Search Budget.
3. **SCI-DEFER:** freeze relationship models only; A6 and Trading V1 remain blocked.

Recommendation: SCI-A. It tests incremental resolution value without creating another estimator contest. Morphology remains non-identifying; no M1/M2 label or probability is created. RT0/RT1 use endpoint cumulative responses, not path integrals. PV-BOTH is diagnostic only.

## Decision 3 — Trading Simulation V1

Select one:

1. **TRADE-A — recommended minimal policy:** no trading-only entry threshold; deterministic M1 peer-catch-up and M2 source-normalization directions; coexistence retained; next eligible open execution; resolution-zero-crossing or 10 eligible sessions; equal notional; gross cap 1.0; no borrowing; 10% per-security cap; conflicting legs netted; zero cash return; TC10 one-way primary with TC05/TC20 mandatory sensitivities.
2. **TRADE-B — fixed-ten-session exit:** otherwise identical to A but no early resolution exit. Simpler, less mechanism-aligned.
3. **TRADE-DEFER:** complete scientific V1 without economic validation; inconsistent with the stated five-day Research + Trading V1 objective.

Recommendation: TRADE-A. Entry occurs only when upstream evidence supports a deterministic mapping and all eligibility/quality rules pass. U is not No Trade by definition, but unresolved/conflicting states that prevent such a mapping are V1 trading-ineligible. No trade is forced.

For entry, failure to trade on the immediately next exchange session means no entry; there is no delayed-entry search. For exit, suspension or untradeability delays execution to the first qualified observed session and the delay is reported; `UNKNOWN MISSINGNESS` makes the episode economically unevaluable rather than generating a fill. Qualified C04 coverage must span the full holding interval.

## Decision 4 — post-approval action authority

If C04-A + MODEL-A/C + SCI-A/B + TRADE-A/B are approved, authorize one successor action limited to:

1. acquire/reconstruct only the explicitly approved official C04 action-calendar companion and create a versioned descendant metadata layer with ancestry to `CORE-DATASET-FREEZE-V1`;
2. implement the frozen seven-tuple scientific registry, eligibility masks, TP2/CG2 semiannual inner folds, A1–A6 targets/metrics, and the selected G5 simulation;
3. run tests and deterministic regeneration checks;
4. execute and inspect only authorized 2015–2019 semiannual inner-development evidence;
5. materialize immutable/checksummed scientific and trading artifacts;
6. stop at `PRE-OUTER V1 GATE / RESEARCHER DECISION REQUIRED`.

This successor authority must continue to deny OF4 and final held-out access. It must not acquire P1 fundamentals, execute R2/R5, add candidates, or repair scientific underperformance through protocol changes.

## Approved bundle

The researcher approved `C04-A + MODEL-A + SCI-A + TRADE-A`, plus the bounded post-approval Phase 1 and inner-development action. The binding freeze record is [V1 Pre-Computation Trading Protocol Freeze](../../decisions/V1_PRECOMPUTATION_TRADING_PROTOCOL_FREEZE.md).

## Repository and validation state

The researcher authorized a normal non-force commit and push of the frozen protocol before Phase 1 begins. `.venv/` remains unrelated and excluded. Validation must pass for Literature OS, documentation architecture, Learning Layer, core freeze, agent control, the V1 gate invariants, JSON contracts, and `git diff --check`. `CORE-DATASET-FREEZE-V1` remains unchanged at root fingerprint `3952FC92E5AB88787E82AE5629609C87150035A449A3D31C6030D0ADEE0C3616`.

`PRE-COMPUTATION + TRADING-PROTOCOL V1 GATE — APPROVED / FROZEN`
